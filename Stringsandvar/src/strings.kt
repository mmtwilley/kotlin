fun main(args: Array<String>){
    println("A string is a chain of zero or more characters surrounded by double quotes.")
    println("They can contain any letter, number, character or symbol except double quotes and back slash")
    println("The answer is \"yes\" so you can procreed!")
    /**
     * a character is escaped with a backslash \
     * if a string contains a backslash, it needs to be escaped
     */
    println("I like to put a backslash \\ in my code!")
    // A double backslash in a string is interpreted as a single backslash.
    /**
     * A char is a single character
     * surround by single quotes
     * A string is composed of characters(string of chars)
     */
    println("The string below is a char")
    print('A')
}